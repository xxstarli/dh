# CODEX_TASK.md

# 个人导航网站 V1｜Codex 开发任务说明

> 本文件是 Codex 开发入口文件。  
> 在开始写任何代码之前，必须先完整阅读本文件及指定文档。  
> 本项目目标是完成一个可直接投入个人日常使用的 V1，而不是只完成静态 UI Demo。

---

# 1. 项目目标

开发一个：

> **个人自用、简洁、快速、低维护成本的网址导航网站 V1**

核心体验：

```text
打开首页
  ↓
直接看到全部分类及网站
  ↓
搜索 / 点击网站
  ↓
新标签页打开目标网站
```

管理体验：

```text
首页
  ↓
点击“管理”
  ↓
输入管理密码
  ↓
直接在当前首页管理
  ↓
添加 / 编辑 / 删除 / 排序
  ↓
完成管理
```

项目不需要传统复杂后台。

---

# 2. 开发前必须阅读的文件

请严格按以下顺序阅读。

## 第一优先级

### 01_PRD.md

业务需求单一事实源（SSOT）。

重点确认：

- V1 做什么
- V1 不做什么
- 首页结构
- 五列布局
- 搜索规则
- 分类管理
- 网站管理
- 管理模式

如果其他文件与 PRD 存在业务冲突：

> **以 PRD 为准。**

---

## 第二优先级

### 02_UI与交互说明.md

用于确认：

- 首页视觉结构
- 卡片布局
- Hover
- 搜索交互
- 添加 / 编辑网站弹窗
- 管理模式
- 菜单
- 拖拽
- 响应式
- Loading / Error / Empty

UI 图是视觉参考。

不要根据截图自行发明业务功能。

---

## 第三优先级

### 03_数据结构与业务规则.md

用于确认：

- Category
- Site
- Settings
- 管理 Session
- URL 规则
- Favicon
- 图标上传
- 排序
- 删除约束
- 服务端鉴权
- API 行为
- SSRF 安全

---

## 第四优先级

### 04_开发验收清单.md

开发完成后必须逐项执行。

不能只阅读。

要求：

> 检查 → 发现问题 → 直接修复 → 重新检查

---

# 3. UI 参考文件

开发前还需要查看：

```text
docs/ui/导航网站_首页UI_V1.png
docs/ui/导航网站_添加编辑网站弹窗UI_V1.png
docs/ui/导航网站_管理模式UI_V1.png
```

如果实际目录名称不同：

> 以项目中提供的对应三张 UI 图为准。

三张图分别用于参考：

1. 首页
2. 添加 / 编辑网站弹窗
3. 管理模式

UI 图里的：

- 网站名称
- 网站数量
- 分类名称
- 图标
- 示例数据

均属于视觉示例。

不得硬编码为产品核心数据。

---

# 4. 开发原则

整个开发过程必须遵循：

## 4.1 简洁优先

不要为了“架构完整”引入不必要的复杂度。

本项目是：

> 小型个人自用工具

不是企业 SaaS。

---

## 4.2 严格控制 V1 范围

不要自行增加 PRD 没有定义的功能。

尤其不要增加：

- 左侧分类栏
- 二级分类
- 收藏
- 最近访问
- 浏览次数
- 热门排行
- 推荐系统
- 标签系统
- 评论
- 用户投稿
- 用户注册
- 多管理员
- 角色权限系统
- 独立管理后台
- 数据统计后台
- 操作日志
- 回收站
- 消息通知
- 网站监控
- AI 功能
- 搜索引擎聚合
- 复杂主题系统

如果发现“未来可能需要”：

> 不要现在实现。

---

# 5. 推荐技术栈

优先使用成熟、简单、维护成本低的技术方案。

推荐：

```text
Next.js
TypeScript
Tailwind CSS
shadcn/ui（按需使用）
```

数据层推荐：

```text
Prisma
```

数据库根据实际部署方式选择：

## 如果部署到自有 Linux 服务器

推荐：

```text
SQLite
```

适合：

- 单用户
- 数据量小
- 运维简单

---

## 如果部署到 Vercel / 无状态云平台

推荐：

```text
PostgreSQL
```

例如：

- Supabase PostgreSQL
- Neon
- 其他托管 PostgreSQL

---

# 6. 技术栈选择原则

如果项目已经存在技术栈：

> 优先沿用现有项目。

不要为了推荐技术栈推倒已有合理代码。

如果项目尚未初始化：

> 选择最简单、最稳定、最适合当前部署环境的方案。

禁止为了该项目引入：

- 微服务
- Redis
- Kafka
- 消息队列
- Kubernetes
- 多数据库
- GraphQL（无明确必要）
- 独立前后端双项目
- 复杂容器编排

---

# 7. 推荐目录结构

可以根据框架实际情况调整，但应保持清晰。

参考：

```text
/
├─ app/
│  ├─ api/
│  ├─ page.tsx
│  └─ ...
│
├─ components/
│  ├─ navigation/
│  ├─ admin/
│  ├─ dialogs/
│  └─ ui/
│
├─ lib/
│  ├─ db/
│  ├─ auth/
│  ├─ validation/
│  ├─ favicon/
│  └─ utils/
│
├─ prisma/
│  ├─ schema.prisma
│  └─ seed.ts
│
├─ public/
│
├─ docs/
│  ├─ 01_PRD.md
│  ├─ 02_UI与交互说明.md
│  ├─ 03_数据结构与业务规则.md
│  ├─ 04_开发验收清单.md
│  └─ ui/
│
├─ .env.example
├─ README.md
└─ CODEX_TASK.md
```

不要为了“组件化”拆出大量无意义小文件。

也不要把整个页面和全部业务写进一个超大文件。

保持：

> 易读、易维护、边界清楚。

---

# 8. 开发顺序

建议严格按以下顺序实现。

---

## Phase 1｜项目基础

完成：

- 项目初始化
- TypeScript
- Tailwind
- 数据库
- Prisma / ORM
- 环境变量
- Migration
- Seed
- 基础目录

确认：

> 项目可以启动。

---

## Phase 2｜数据模型

根据 `03_数据结构与业务规则.md` 实现：

- Category
- Site
- Settings

以及必要关联关系。

不要增加无关业务表。

---

## Phase 3｜公共首页数据

实现：

- 首页数据读取
- 分类排序
- 网站排序
- 空分类数据处理

建议一次性返回首页所需数据。

---

## Phase 4｜首页 UI

根据：

- PRD
- UI 与交互说明
- 首页 UI 图

实现：

- 顶部
- 搜索
- 分类
- 网站卡片
- PC 五列
- 响应式

此阶段先完成普通浏览模式。

---

## Phase 5｜搜索

实现：

- 网站名称搜索
- 网站说明搜索
- 网站域名搜索
- 实时过滤
- 空结果
- 清空搜索

搜索优先前端完成。

---

## Phase 6｜管理鉴权

实现：

- 管理密码
- 服务端校验
- 密码哈希
- Session
- HttpOnly Cookie
- 管理登录
- 管理退出
- 写接口鉴权

不要只在前端做权限判断。

---

## Phase 7｜分类管理

实现：

- 添加分类
- 编辑分类
- 删除空分类
- 阻止删除非空分类
- 分类拖拽排序

所有操作持久化。

---

## Phase 8｜网站管理

实现：

- 添加网站
- 编辑网站
- 删除网站
- 修改所属分类
- 分类内排序

严格遵循业务规则：

> 网站不可通过拖拽跨分类。

---

## Phase 9｜网站图标

实现：

- 自动获取 Favicon
- 默认图标
- 自定义上传
- 恢复自动获取
- 获取失败降级

如果服务端抓取 URL：

> 必须实现 SSRF 防护。

---

## Phase 10｜管理模式 UI

根据管理模式 UI 图实现：

- 管理状态提示
- 分类拖拽手柄
- 分类菜单
- 网站拖拽手柄
- 网站菜单
- 添加网站
- 添加分类
- 完成管理

管理模式必须：

> 继续使用原首页。

---

## Phase 11｜异常状态

完善：

- Loading
- Error
- Empty
- 保存失败
- 排序失败回滚
- 上传失败
- Favicon 失败
- URL 错误
- 未授权

---

## Phase 12｜响应式

检查：

- 5 列
- 4 列
- 3 列
- 2 列
- 顶部布局
- 弹窗
- 菜单

---

## Phase 13｜最终验收

完整执行：

```text
04_开发验收清单.md
```

发现问题：

> 直接修复。

修复后：

> 重新测试。

---

# 9. 首页硬性要求

以下属于硬性规则，不得自行修改：

1. 首页直接显示全部分类。
2. 分类下直接显示全部网站。
3. 不做左侧分类栏。
4. 不做二级分类。
5. 不做分页。
6. 不做“查看更多”。
7. 常规 PC 每行最多 5 个网站。
8. 不足 5 个从左往右排列。
9. 不得自动放大卡片填满一行。
10. 点击网站在新标签页打开。

---

# 10. 管理模式硬性要求

1. 普通首页点击“管理”。
2. 输入管理密码。
3. 验证通过进入管理模式。
4. 仍然停留在原首页。
5. 不跳独立管理后台。
6. 可添加 / 编辑 / 删除 / 排序。
7. 所有操作即时保存。
8. “完成管理”仅退出管理状态。
9. 不设计“保存全部”。

---

# 11. 分类硬性规则

必须：

- 一级分类
- 名称必填
- 可重名
- 新增排最后
- 可拖拽排序
- 空分类可删除
- 非空分类不可删除

禁止：

> 删除分类时级联删除网站。

---

# 12. 网站硬性规则

网站字段：

```text
名称
URL
图标
说明
所属分类
```

其中：

```text
名称：必填
URL：必填
图标：选填
说明：选填
分类：必填
```

不要增加更多表单字段。

---

# 13. URL 规则

用户输入：

```text
example.com
```

自动变为：

```text
https://example.com
```

仅允许：

```text
http
https
```

禁止危险协议。

URL 不唯一。

同一个网站允许出现在多个分类。

---

# 14. 网站排序规则

新网站：

> 添加到分类最后。

同分类编辑：

> 保留原位置。

修改分类：

> 移动到新分类最后。

拖拽：

> 只允许当前分类内部。

---

# 15. Favicon 规则

优先级：

```text
自定义上传
   ↓
自动 Favicon
   ↓
系统默认图标
```

必须保证：

> 自动 Favicon 获取失败不会阻止保存。

---

# 16. 数据持久化要求

以下数据必须持久化：

- 分类
- 网站
- 分类顺序
- 网站顺序
- Settings
- 自定义图标

刷新页面后：

> 数据必须保持。

关闭浏览器重新打开：

> 数据必须保持。

禁止使用：

> localStorage 作为最终数据库。

---

# 17. 鉴权安全要求

管理密码：

- 不得明文存储
- 不得写死在前端
- 不得提交真实密码到 Git

写接口：

> 必须服务端验证管理权限。

Session：

推荐使用：

```text
HttpOnly Cookie
```

HTTPS 环境启用：

```text
Secure
```

---

# 18. Favicon SSRF 要求

如果服务器主动访问用户输入的网址，至少防护：

- localhost
- 127.0.0.1
- 私有 IP
- 云 metadata 地址
- 无限重定向
- 超时
- 超大响应

不要因为这是个人工具而忽略 SSRF 风险。

---

# 19. UI 实现原则

参考 UI 图，但不要机械像素硬编码。

重点还原：

- 页面结构
- 内容宽度
- 五列卡片
- 留白
- 圆角
- 卡片高度
- 字号层级
- 搜索框
- 按钮
- 弹窗
- 管理状态

整体风格应：

> 简洁、明亮、轻量、工具型。

不要擅自改成：

- 深色后台
- 企业管理系统
- Portal 门户
- 复杂仪表盘

---

# 20. 组件原则

建议拆分：

```text
Header
SearchBar
CategorySection
SiteCard
AdminModeBanner
AdminLoginDialog
CategoryDialog
SiteDialog
ConfirmDialog
SiteMenu
CategoryMenu
SortableCategory
SortableSite
```

可根据实际项目调整。

原则：

- 避免一个组件超过合理体积
- 避免重复代码
- 避免为了“抽象”增加复杂泛型和框架

---

# 21. 类型与校验

必须使用 TypeScript。

推荐前后端共享或保持一致的校验规则。

可使用：

```text
Zod
```

或当前框架已有成熟方案。

所有客户端校验：

> 服务端必须再次校验。

---

# 22. 数据库 Migration

必须：

- 数据库 Schema 可版本管理
- Migration 可以从干净数据库运行
- 不依赖手工改数据库

如果存在 Seed：

> Seed 只用于初始化 / 开发数据。

不要把 UI 图中的示例网站强制写入生产。

---

# 23. 环境变量

项目必须提供：

```text
.env.example
```

只写变量名和示例格式。

不得包含：

- 真实密码
- 生产密钥
- 数据库真实凭证

README 中说明每个变量用途。

---

# 24. 不要做的技术设计

除非现有项目已经使用，否则不要主动加入：

- Redux（本项目通常不需要）
- 复杂全局状态框架
- GraphQL
- WebSocket
- Redis
- 消息队列
- 多租户
- CQRS
- Event Sourcing
- 微服务
- Docker Compose 多服务架构
- Kubernetes

V1 目标是：

> 简单稳定。

---

# 25. 错误处理

所有关键操作必须有失败状态。

至少处理：

```text
分类保存失败
网站保存失败
分类删除失败
网站删除失败
分类排序失败
网站排序失败
上传失败
Favicon 获取失败
管理密码错误
管理 Session 失效
数据库异常
```

禁止：

> 请求失败后页面无反馈。

---

# 26. 排序失败

建议使用：

> 乐观更新 + 失败回滚。

流程：

```text
拖动
 ↓
前端显示新顺序
 ↓
调用接口
 ↓
成功 → 保持
失败 → 恢复原顺序 + 提示
```

---

# 27. 测试要求

至少测试完整业务流程：

```text
普通浏览
搜索
管理登录
添加分类
编辑分类
删除分类
分类排序
添加网站
编辑网站
删除网站
网站换分类
网站排序
退出管理
刷新持久化
```

不要只测试单个 API。

---

# 28. 浏览器测试

至少验证：

```text
Chrome 最新版
Edge 最新版
```

建议检查：

```text
Safari
手机浏览器
```

---

# 29. 代码质量要求

完成后执行项目已有：

```text
lint
typecheck
build
test
```

如果项目未配置某一项：

> 不需要为了本项目搭复杂测试平台，但至少保证 TypeScript、构建和关键流程正常。

不得留下：

- 大量 TypeScript error
- 明显 ESLint error
- 无用 console.log
- 测试假数据写死在生产页面
- TODO 阻塞核心功能

---

# 30. 完成后必须自检

开发完成后打开：

```text
04_开发验收清单.md
```

逐项检查。

规则：

```text
失败
 ↓
定位原因
 ↓
直接修复
 ↓
重新测试
```

不要输出：

> “建议后续优化”

来替代 V1 本应完成的功能。

---

# 31. 最终交付前必须执行

至少执行：

```text
安装依赖
数据库初始化
Migration
项目启动
Lint
Typecheck
Build
核心流程测试
```

确保项目在干净环境下具备可运行性。

---

# 32. README 要求

项目最终必须有清晰 README。

至少包含：

## 项目介绍

一句话说明项目用途。

## 技术栈

列出实际使用技术。

## 本地运行

例如：

```text
1. 安装依赖
2. 创建 .env
3. 初始化数据库
4. 启动开发环境
```

## 数据库

说明：

- Migration
- Seed
- 数据文件 / 数据库位置

## 环境变量

说明用途。

## 管理密码

说明如何初始化或配置。

不得在 README 写真实密码。

## 部署

说明推荐部署方式。

---

# 33. 最终交付报告格式

完成后请输出以下报告。

---

## 1. 开发完成情况

```text
状态：已完成 / 存在未完成项
```

---

## 2. 技术栈

实际使用：

```text
Frontend:
Backend:
Database:
ORM:
UI:
Auth:
Storage:
```

---

## 3. 项目结构

简要说明主要目录。

---

## 4. 数据结构

说明：

```text
Category
Site
Settings
```

以及关系。

---

## 5. 已完成功能

逐项列出：

- 首页
- 搜索
- 分类管理
- 网站管理
- 拖拽排序
- 管理鉴权
- 图标
- 响应式
- 异常状态

---

## 6. 数据持久化

说明：

- 使用什么数据库
- 图标保存在哪里
- 排序如何保存

---

## 7. 本地运行方式

提供准确命令。

---

## 8. 环境变量

列出变量名和用途。

不要输出真实 Secret。

---

## 9. 部署方式

说明当前项目最适合如何部署。

---

## 10. 验收结果

根据：

```text
04_开发验收清单.md
```

输出：

```text
已通过：
未通过：
无法自动验证：
```

---

## 11. 未完成项

如果无：

```text
无未完成项。
```

如果有：

必须明确：

- 问题
- 原因
- 影响

不能把核心未完成项包装为“后续优化”。

---

# 34. 遇到文档歧义时的处理

优先级：

```text
01_PRD.md
  ↓
03_数据结构与业务规则.md
  ↓
02_UI与交互说明.md
  ↓
UI 参考图
```

如果只是视觉细节未明确：

> 采用最简洁、最符合现有 UI 风格的方案。

如果涉及业务逻辑：

> 不自行扩展功能。

---

# 35. 最终开发目标

完成后的产品应该做到：

### 普通状态

> 打开就是一个干净、快速的网址导航首页。

### 管理状态

> 原首页直接进入可编辑状态，可以快速管理分类和网站。

### 技术层面

> 数据真实持久化、鉴权有效、排序可靠、异常有反馈、项目可以稳定运行。

最终交付的应该是：

> **可以直接投入个人日常使用的导航网站 V1。**

而不是：

> 静态页面、UI Demo 或只有前端假数据的原型。
